import { useAuth } from '../context/AuthContext';

// This hook is a direct re-export of the AuthContext hook
// keeping it here for consistency with the hooks/ folder structure
export default useAuth;
